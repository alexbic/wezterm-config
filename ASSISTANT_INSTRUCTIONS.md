
## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #1 (НАРУШЕНИЕ = ПРОВАЛ ЧАТА)

### ЗАПРЕТ НА `!` РЯДОМ С КАВЫЧКАМИ:
* **НИКОГДА** не ставить `!` перед закрывающей кавычкой в heredoc
* **НИКОГДА**: `echo "текст!"` или `"текст!"`  
* **ВСЕГДА**: `echo "текст\!"` или переменные
* **ПРОВЕРКА ОБЯЗАТЕЛЬНА** перед каждым heredoc на наличие `!`
* **ЭТО RULE #1** - важнее экономии токенов

### ПОСЛЕДСТВИЯ НАРУШЕНИЯ:
- `history expansion` в bash
- `command dquote>` зависание терминала  
- Трата токенов на объяснения одной ошибки
- Потеря доверия оператора

# 🤖 ИНСТРУКЦИИ ДЛЯ AI АССИСТЕНТА - WEZTERM CONFIG PROJECT (ОБНОВЛЕНО 2025-06-10) 🚀

## Введение
Данный документ — основное руководство для AI-ассистента по работе над проектом WezTerm. Цель: обеспечить **эффективное, экономичное и безопасное выполнение задач**.

---

## I. 👤 ОПЕРАТОР И КОНТЕКСТ

### 1.1. Профиль Оператора
* **Имя:** Александр
* **Язык:** Русский 🇷🇺
* **Платформа:** macOS 🍎
* **Экспертиза (WezTerm):** Средняя 🧠
* **Предпочтения:** Порядок, эмодзи, дотошность. ✨

### 1.2. Приоритеты
* **КРИТИЧНО:** Экономия токенов 💰
* **Высокий:** Точность решений ✅
* **Стиль:** Сжатые команды, итоговый результат. ⚡

---

## II. 💬 УПРАВЛЕНИЕ ДИАЛОГОМ И КОММУНИКАЦИЯ

### 2.1. Лимиты Сообщений
* **Тип ассистента:** Gemini
* **Лимит:** 16 сообщений на диалог.
* **Счётчик:** `#X/16` в каждом ответе.
* **8 сообщений:** Первое предупреждение. ⚠️
* **10+ сообщений:** Завершать текущую задачу.
* **14+ сообщений:** Подготовка к завершению диалога, сбор результатов. 🚨

### 2.2. Стиль и Экономия Токенов (КРИТИЧНО)
* **Краткость:** Максимально сжатое, но полное изложение. 🤏
* **Без повторов:** Избегать дублирования информации.
* **Целевые ответы:** Строго по вопросу, минимум "воды". 🎯
* **Эффективность команд:** Автономные и эффективные Bash-команды.
* **Commit:** Подробный, с историей чата.
* **Обращение:** По имени "Александр".
* **Адаптация:** Под средний уровень экспертизы WezTerm.

### 2.3. Формат Вывода Команд
* **🟢 ОБЫЧНЫЙ РЕЖИМ (по умолчанию):** `sed` + `echo` **БЕЗ** Markdown блоков в чате.
    ```bash
    sed -i "" "s/проблема/решение/" file.lua
    echo "✅ РЕЗУЛЬТАТ: что исправлено"
    ```
* **🟡 ДЕТАЛЬНЫЙ РЕЖИМ (по запросу "дай детальный блок"):** Развёрнутые объяснения + полная диагностика.

---

## III. 🛠️ ПРАВИЛА ГЕНЕРАЦИИ ИСПОЛНЕНИЯ КОМАНД (КРИТИЧНО!)

### 3.1. Безопасность Markdown и Heredoc
* **Проблема:** Markdown-символы ломают синтаксис.
* **Решение: Ключевые слова для Markdown в файлах.**
    * **В файлах:**
        * ````bash` (для Bash-кода)
        * ````lua` (для Lua-кода)
        * ````text` (для текста)
        * ````` (завершение блока)
    * **Для описания этих слов в тексте инструкций:**
        * `__MD_BASH_START__`
        * `__MD_LUA_START__`
        * `__MD_TEXT_START__`
        * `__MD_CODE_END__`
    * **Пример переменных для использования в Heredoc:**
        ```bash
        MD_BASH_CODE="```bash"
        MD_LUA_CODE="```lua"
        MD_TEXT_CODE="```text"
        MD_END_CODE="```"
        ```
* **Безопасный синтаксис Heredoc:**
    * Метка (`<< МЕТКА`) **БЕЗ кавычек** для интерпретации переменных.
    * Метка (`<< 'МЕТКА'`) **В кавычках** для буквальной передачи содержимого (например, для `read -r -d '' VAR << 'МЕТКА'`).
    * Уникальность метки `heredoc` для избежания преждевременного завершения.
    * **НИКОГДА** не использовать Heredoc с прямыми Markdown блоками (```).

### 3.2. Обработка Специальных Символов
* **`!` (восклицательный знак):** Может вызвать "history expansion" в оболочке.
    * **НЕПРАВИЛЬНО:** `echo "текст!"`
    * **ПРАВИЛЬНО:** `echo "текст\!"` или через переменную:
        ```bash
        EXCLAMATION_MARK='!'
        echo "текст${EXCLAMATION_MARK}"
        ```
    * В Heredoc с кавычками (`<< 'EOF'`), экранирование `!` не требуется.

### 3.3. Определение Многострочных Переменных
* **Рекомендация:** `read -r -d '' VAR << 'МЕТКА'` для точного сохранения содержимого.

### 3.4. Шаблон: Безопасное и Надежное Обновление Файлов / Вставка Контента
* **Проблемы:** Экранирование, `\` в Zsh (`Segmentation Fault`), `sed` regex ошибки.
* **Решение: Шаблон с временным файлом и `sed 'r'`:**
    1.  **Создать уникальный временный файл:** `TEMP_FILE="/tmp/my_content_$$.tmp"`
    2.  **Записать контент во временный файл (литерально):** `cat > "$TEMP_FILE" << 'EOF_MARKER'`
        * **КРИТИЧНО:** Метка `EOF_MARKER` **в одинарных кавычках**.
        * **КРИТИЧНО:** `\` (обратный слеш) **НЕ ДОЛЖЕН быть последним символом в строке** перед переводом строки в heredoc. Добавить пробел или другой символ.
    3.  **Экранировать маркер поиска для `sed`:**
        ```bash
        INSERT_MARKER_ESCAPED=$(echo "$RAW_MARKER" | sed 's/[][\/.^$*()]/\\&/g')
        ```
    4.  **Вставить контент из временного файла:** `sed -i '' "/$INSERT_MARKER_ESCAPED/r $TEMP_FILE" "ваш_файл.md"`
    5.  **Очистить временный файл:** `rm "$TEMP_FILE"`
* **Пример тестовой вставки (для демонстрации):**
    ```bash
    read -r -d '' COMPLEX_CONTENT << 'EOM_CONTENT'
    # 🚀 ТЕСТОВАЯ ИНСТРУКЦИЯ
    ## Проблемная секция с кодом Bash
    ```bash
    # Команда с heredoc (вложенный heredoc для Lua-файла)
    cat > file.lua << 'INNER_LUA_EOF'
    local wezterm = require('wezterm')
    -- комментарий: кавычки экранируются символом обратного слеша. Пример: "test"
    return { test = true }
    INNER_LUA_EOF
    ```

    ## Lua код секция
    ```lua
    local M = {}
    M.function_with_quotes = function()
      return "test string with quotes"
    end
    return M
    ```

    ## Текстовая секция
    ```text
    Это простой текстовый блок.
    Он может содержать *курсив* или **жирный** текст.
    ```
    EOM_CONTENT

    # Вставка в тестовый файл (пример, реальный файл будет ASSISTANT_INSTRUCTIONS.md)
    # cat > "$HOME/.config/wezterm/complex_test.md" << EOF_FINAL
    # $COMPLEX_CONTENT
    # EOF_FINAL
    ```

### 3.5. Шаблон: Безопасная Замена Блока Текста через `awk`
* **Проблема:** Замена целого блока между маркерами, избегая ошибок `sed`.
* **Решение:**
    1.  **Подготовить новый контент в `TEMP_NEW_BLOCK_FILE`:** Использовать `cat > "$TEMP_NEW_BLOCK_FILE" << 'EOF_LITERAL'` (метка в кавычках).
    2.  **Определить точные строковые `START_MARKER` и `END_MARKER`.**
    3.  **Использовать `awk` для замены:**
        ```bash
        awk -v new_content_file="$TEMP_NEW_BLOCK_FILE" \
            -v start_m="$START_MARKER_AWK" \
            -v end_m="$END_MARKER_AWK" '
            BEGIN { in_block = 0; }
            {
                if ($0 == start_m) { in_block = 1; print $0; while ((getline line < new_content_file) > 0) { print line; } close(new_content_file); next; }
                if ($0 == end_m) { in_block = 0; print $0; next; }
                if (in_block == 1) { next; }
                print $0;
            }
            ' "$TARGET_FILE" > "$TEMP_OUTPUT_FILE"
        mv "$TEMP_OUTPUT_FILE" "$TARGET_FILE"
        ```
    4.  **Очистить временные файлы:** `rm "$TEMP_NEW_BLOCK_FILE"` и `rm "$TEMP_OUTPUT_FILE"`.

### 3.6. Контроль Вывода в Чат
* **НИКОГДА:** Не выводить содержимое файлов напрямую в чат, если оно может нарушить форматирование или содержит спецсимволы. 🚫
* **ВМЕСТО ЭТОГО:** Инструктировать оператора проверить файл самостоятельно.

---

## IV. 🏛️ АРХИТЕКТУРНЫЕ ПРИНЦИПЫ ПРОЕКТА WEZTERM

### 4.1. Общие Принципы
* **Кроссплатформенность:** Скрипты для macOS/Linux/Windows.
* **Локализация:** Учёт локализации.
* **Cross-platform `sed`:** Использование кроссплатформенных функций `sed`.
* **Создание файлов:** Через `tee` БЕЗ Markdown блоков.
* **F10 Архитектура:** Соблюдать `config/dialogs/ ←→ utils/dialogs.lua`.
* **Централизация:** Иконок, цветов, локализации.
* **При неопределенности:** `web_search` первым делом. 🌐

### 4.2. Структура Кода и Функции (Чистота Кода)
* **Функции:** `config/` = данные, `utils/` = функции.
* **НИКОГДА:** Функции в `config/`.
* **НИКОГДА:** Добавлять функции по частям - делать всё сразу в одном блоке.
* **Эмодзи:** Только `environment.icons.t.key` (централизованно), **НЕ** прямые эмодзи в `config/`.

### 4.3. Диалоговые Системы
* **Типы:** `InputSelector` (выбор), `PromptInputLine` (ввод текста).
* **Структура:** `config/dialogs/name.lua` = данные, `utils/dialogs.lua` = функции.
* **Цвета:** Использовать `input_selector_label_bg/fg` в `colors` для `InputSelector`.
* **Ограничение:** Hover-эффекты НЕ ПОДДЕРЖИВАЮТСЯ.
* **Локализация:** Анализировать существующие ключи перед созданием `config`.

### 4.4. Работа с Событиями WezTerm (КРИТИЧНО!)
* **НЕ ТРОГАТЬ:** `events/tab-title.lua` без крайней необходимости.
* **НЕ ЛОМАТЬ:** Структуру `if-then-else` в обработчиках.
* **НЕ ДОБАВЛЯТЬ:** Номера в названия вкладок (вызывает `FormatColor` ошибки).
* **ВСЕГДА:** Проверять `luac -p` после КАЖДОГО изменения событий.
* **Меры предосторожности:**
    * **EMERGENCY BACKUP:** Перед правкой событий.
    * **НЕМЕДЛЕННЫЙ ОТКАТ:** При ошибках или бесконечном логировании.
* **Сложные изменения:**
    * **НЕ ИСПОЛЬЗОВАТЬ:** Сложные `sed` блоки для событий.
    * **НЕ НАРУШАТЬ:** Структуру функций в событиях.

---

## V. ⚙️ ОПЕРАЦИОННЫЕ ПРОЦЕДУРЫ И ПРИНЯТИЕ РЕШЕНИЙ

### 5.1. Иерархия Приоритетов (КРИТИЧНО!)
При конфликтах, решения принимаются по иерархии:
1.  **Экономия токенов** 💰
2.  **Безопасность синтаксиса** ✅
3.  **Критические правила (разделы 3.1, 3.2, 3.4, 4.4)** 🛡️
4.  **Архитектурная чистота** 🏛️
5.  **Анализ документов** 📄
6.  **`cross_platform` функции**
7.  **Один мега-блок**

### 5.2. Алгоритм Завершения Чатов (при 14+ сообщениях)
1.  Анализ новых ошибок чата.
2.  Обновление `ASSISTANT_INSTRUCTIONS.md`.
3.  Обновление `ROADMAP.3.2.1.2.md`.
4.  `git commit` + `push`. 🔄

### 5.3. Правила Git Commit
* После крупных исправлений или завершения логического блока.
* **Формат:** `CHAT #X: описание изменения`.

### 5.4. Анализ и Очистка (Борьба с Хаосом)
* Анализ текущего состояния критичен перед началом новых функций.
* **ПРИОРИТЕТ:** Экстренная очистка/исправление существующих проблем. 🧹

---

## VI. 📚 КЛЮЧЕВЫЕ УРОКИ (Для быстрого референса)

## VI. 📚 КЛЮЧЕВЫЕ УРОКИ (Для быстрого референса)

### ⚠️ НОВЫЕ КРИТИЧЕСКИЕ УРОКИ ИЗ ЧАТА #37:
* **Универсальные диалоги:** state_provider ДОЛЖНА возвращать { action = "refresh/exit/none" }
* **Перерисовка диалогов:** Только через wezterm.time.call_after(0.1) для избежания краша
* **Разделители InputSelector:** НЕ ДОЛЖНЫ закрывать диалог, только игнорироваться
* **M.build_inputselector:** Цвета через wezterm.format() для включенных элементов
* **Сложные sed замены:** ВСЕГДА через temp файлы + awk для безопасности
* **Emergency backup:** ОБЯЗАТЕЛЕН перед любыми изменениями dialog/events файлов
* **Архитектура диалогов:** config/dialogs/=данные, utils/dialogs.lua=функции, state_provider=логика
* **Протокол завершения:** Анализ ошибок → обновление ASSISTANT_INSTRUCTIONS → ROADMAP → git commit


* **Один мега-блок:** Команды + переменные + проверка результата.
* **Результат в конце:** Только итоговый `echo`.
* **Без лишних комментариев:** Только необходимые пояснения.
* **Не разбивать:** На множество мелких блоков.
* **События WezTerm:** Критичны для UI, ошибки вызывают краш.
* **Не создавать избыточные тестовые файлы:** Использовать существующие.
* **Проверка `locale` ключей:** Критична перед созданием `dialog config`.
* **Анализ хаоса:** Критичен перед началом разработки.
* **Функции:** `config/` = данные, `utils/` = функции.
* **Всегда перезаписывать:** Целиком проблемные файлы из документов.
* **Использовать существующую архитектуру:** Вместо создания дублей.
* **Экстренная очистка:** Приоритет перед новыми функциями.
* **Не трогать `events/tab-title.lua`:** Без backup + понимания структуры.
* **Не использовать сложные `sed` блоки для событий:** Вызывают синтаксические ошибки.
* **Не нарушать структуру функций в событиях.**
* **Использовать официальные цвета:** `input_selector_label_bg/fg`.
* **Всегда проверять `luac -p`:** После КАЖДОГО изменения событий.
* **Экстренный откат:** При любых ошибках в событиях.


## 🚨 НОВЫЕ КРИТИЧЕСКИЕ ПРАВИЛА ИЗ ЧАТА #44:

### ПОЛНЫЙ ЗАПРЕТ sed ДЛЯ ЗАМЕН:
- ❌ **НИКОГДА** не использовать `sed -i` для замены функций/блоков
- ❌ **НИКОГДА** не использовать `sed` с regex для сложных замен
- ✅ **ТОЛЬКО** безопасный протокол: temp file + awk + mv
- ✅ **ВСЕГДА** emergency backup перед изменениями

### УНИВЕРСАЛЬНЫЕ ДИАЛОГИ:
- config/dialogs/ = ТОЛЬКО данные (meta, main_items, service_items)
- utils/dialogs.lua = M.build_inputselector(wezterm, config, state_provider)
- Изменения: только вызовы в биндингах, НЕ создание новых функций
- Тестирование после КАЖДОЙ замены обязательно


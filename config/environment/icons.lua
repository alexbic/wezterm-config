-- cat > ~/.config/wezterm/config/environment/icons.lua << 'EOF'
--
-- ОПИСАНИЕ: Константы иконок для категорий сообщений WezTerm
-- Определяет единую систему символов для всех типов логирования и сообщений.
-- Цвета вынесены в отдельный файл config/environment/colors.lua
-- Функции для работы с этими данными находятся в utils/environment.lua
--
-- ЗАВИСИМОСТИ: НЕТ

-- === ИКОНКИ ПО КАТЕГОРИЯМ ===
-- Символические иконки для различных типов сообщений
local ICONS = {
  -- === КАТЕГОРИИ СООБЩЕНИЙ ДЛЯ ЛОГИРОВАНИЯ ===
  system = "⚙",        -- системные сообщения
  platform = "⌘",      -- платформа и окружение
  ui = "▣",           -- интерфейс терминала
  tip = "ⓘ",          -- подсказки и советы
  mode = "◐",         -- режимы работы
  time = "◷",         -- время и дата
  appearance = "※",    -- внешний вид
  input = "⌜",        -- пользовательский ввод
  session = "ⓢ",      -- система сессий
  workspace = "󱂬",    -- управление workspace (Nerd Font)
  window = "",        -- окна (Nerd Font)
  tab = "󰓩",           -- вкладки (Nerd Font)
  debug = "⊠",        -- отладка
  error = "✗",        -- ошибки
  exit = "⏏",          -- выход/закрытие
  
  -- === ИКОНКИ ДЛЯ ОТЛАДКИ РЕЖИМОВ ===
  -- Специальные индикаторы состояния режимов
  mode_activated = "🎯",     -- активация режима
  mode_deactivated = "🚪",   -- деактивация режима
  timeout_exit = "⏱️",        -- завершение по таймауту
  manual_exit = "👆",        -- ручное завершение  
  
  -- === ИКОНКИ ДЛЯ СЛУЖЕБНЫХ ОКОН ===
  -- Окна которые перекрывают экран (InputSelector, PromptInputLine)
  debug_panel_tab = "🪲",         -- панель отладки
  list_picker_tab = "☰",          -- список для выбора (зеленый)
  list_delete_tab = "☰",          -- список для удаления (красный)
  save_workspace_tab = "󱂬",      -- диалог сохранения workspace
  save_window_tab = "",        -- диалог сохранения window
  save_tab_tab = "󰓩",           -- диалог сохранения tab
  
  -- === ИКОНКИ ДЛЯ РЕЖИМОВ УПРАВЛЕНИЯ ===
  -- Отображаются в строке состояния при активных key tables
  session_control = "ⓢ",   -- управление сессиями
  pane_control = "◫",     -- управление панелями
  font_control = "ƒ",     -- управление шрифтами
  debug_control = "⊠",    -- управление отладкой
  workspace_search = "○", -- поиск workspace
  
  -- === ИКОНКИ ДЛЯ ЗАГОЛОВКОВ ВКЛАДОК ===
  -- Элементы дизайна заголовков вкладок терминала
  tab_default = "⬡",          -- обычная вкладка
  tab_admin = "ﱾ",            -- вкладка с правами администратора (UTF-8: 0xfc7e)
  tab_unseen_output = "",    -- индикатор непрочитанного вывода (UTF-8: 0xf111)
  tab_process = "∼",          -- процесс в вкладке (UTF-8: 0x223c)
  tab_semicircle_left = "",  -- левая полуокружность (UTF-8: 0xe0b6)
  tab_semicircle_right = "", -- правая полуокружность (UTF-8: 0xe0b4)
  tab_circle = "",          -- кружок для уведомлений
}

-- Экспорт только иконок
return {
  ICONS = ICONS,
}
